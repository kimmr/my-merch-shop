<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/forms.css">
</head>

<body>
<%- include('../includes/navigation.ejs') %>
<main>
    <% if(products.length > 0) { %>
        <ul>
            <% products.forEach(cartProducts => { %>
                <li>
                    <h1><%= cartProducts.productId.title %> (<%= cartProducts.quantity %>)
                    </h1>
                    <form action="/cart-delete-item" method="POST">
                        <input type="hidden" value="<%= cartProducts.productId.id%>" name="productId">
                        <button class="btn btn-outline-danger" type="submit">DELETE</button>
                    </form>
                </li>
            <% }) %>
        </ul>
        <div class="form-center">
          <form action="/create-order" method="POST"> 
            <button class="btn btn-outline-info" type="submit">Order</button>
          </form>
        </div>
        <% } else { %>
            <h1>Empty Cart</h1>
        <% } %>
</main>
<%- include('../includes/end.ejs') %>
